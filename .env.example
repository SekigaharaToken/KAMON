# Chain (84532 = Base Sepolia, 8453 = Base Mainnet)
VITE_CHAIN_ID=84532

# Required — WalletConnect
VITE_WALLETCONNECT_PROJECT_ID=       # WalletConnect Cloud project ID

# Token addresses
VITE_SEKI_TOKEN_ADDRESS=             # address — $SEKI parent token
VITE_DOJO_TOKEN_ADDRESS=             # address — $DOJO reward token

# DOJO streak resolution
VITE_DOJO_RESOLVER_ADDRESS=          # address — DojoResolver contract
VITE_DOJO_SCHEMA_UID=                # bytes32 — EAS schema UID for DOJO check-ins

# House NFT bonding curves (ERC-1155 via Mint Club)
VITE_HOUSE_FIRE_ADDRESS=             # address — Honoo (Fire) House NFT
VITE_HOUSE_WATER_ADDRESS=            # address — Mizu (Water) House NFT
VITE_HOUSE_FOREST_ADDRESS=           # address — Mori (Forest) House NFT
VITE_HOUSE_EARTH_ADDRESS=            # address — Tsuchi (Earth) House NFT
VITE_HOUSE_WIND_ADDRESS=             # address — Kaze (Wind) House NFT

# House membership resolver (EAS)
VITE_HOUSE_RESOLVER_ADDRESS=         # address — HouseResolver contract
VITE_HOUSE_SCHEMA_UID=               # bytes32 — EAS schema UID for house membership

# Staking pool
VITE_STAKING_POOL_ADDRESS=           # address — Mint Club staking pool

# Optional
VITE_ALCHEMY_API_KEY=                # Alchemy API key for premium Base RPC
